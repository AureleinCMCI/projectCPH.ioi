'use client';

import { LoginForm } from '../../component/LoginForm';

export default function Page() {
  return (
    <html>
      <body>
        <main>
          <LoginForm />
        </main>
      </body>
    </html>

  );
}
